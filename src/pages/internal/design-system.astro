---
import DesignSystemExample from '../../components/blocks/internal/DesignSystemExample.astro';
import IconSamples from '../../components/blocks/internal/IconSamples.astro';
import LayerStackDemo from '../../components/blocks/internal/LayerStackDemo.astro';
import MotionDemoCard from '../../components/blocks/internal/MotionDemoCard.astro';
import TokenSwatch from '../../components/blocks/internal/TokenSwatch.astro';
import CTA from '../../components/blocks/CTA.astro';
import Callout from '../../components/blocks/Callout.astro';
import FeatureGrid from '../../components/blocks/FeatureGrid.astro';
import Hero from '../../components/blocks/Hero.astro';
import LogoCloud from '../../components/blocks/LogoCloud.astro';
import Stats from '../../components/blocks/Stats.astro';
import PlatformLayersDiagram from '../../components/diagrams/PlatformLayersDiagram.astro';
import LayeredPlatform from '../../components/illustrations/scenes/LayeredPlatform.astro';
import Badge from '../../components/primitives/Badge.astro';
import Button from '../../components/primitives/Button.astro';
import Card from '../../components/primitives/Card.astro';
import Cluster from '../../components/primitives/Cluster.astro';
import Container from '../../components/primitives/Container.astro';
import Divider from '../../components/primitives/Divider.astro';
import Grid from '../../components/primitives/Grid.astro';
import Heading from '../../components/primitives/Heading.astro';
import Section from '../../components/primitives/Section.astro';
import Split from '../../components/primitives/Split.astro';
import Stack from '../../components/primitives/Stack.astro';
import Surface from '../../components/primitives/Surface.astro';
import Text from '../../components/primitives/Text.astro';
import {embeddedAutomotivePlatformLayersDiagram} from '../../data/embedded-and-automotive-platform-layers';
import {layeredPlatformScene} from '../../data/illustrations/layered-platform';
import {memberLogos} from '../../data/member-logos';
import {safetyCriticalPlatformLayersDiagram} from '../../data/safety-critical-platform-layers';
import BaseLayout from '../../layouts/BaseLayout.astro';

type TypeScale = 'display-xl' | 'display-l' | 'heading-xl' | 'heading-l' | 'heading-m';
type Principle = {
  title: string;
  description: string;
};
type SurfaceSample = {
  step: string;
  label: string;
  description: string;
  tokenClass: string;
};
type TypeSample = {
  label: string;
  size: TypeScale;
  sample: string;
};
type TextCard = {
  title: string;
  description: string;
};
type ListCard = TextCard & {
  items: string[];
};
type MotionSample = {
  name: string;
  duration: string;
  usage: string;
};

const principles: Principle[] = [
  {
    title: 'Engineering communication',
    description: 'Design choices should make architecture, boundaries, ownership, evidence, governance, and participation easier to understand.',
  },
  {
    title: 'Architecture before promotion',
    description: 'Explain the engineering problem and system model before presenting capabilities or asking for action.',
  },
  {
    title: 'Reusable when proven',
    description: 'Prefer page-local composition until a pattern has multiple real uses and a stable authoring contract.',
  },
];

const surfaces: SurfaceSample[] = [
  {
    step: '0',
    label: 'Base canvas',
    description: 'Page background and deepest system layer.',
    tokenClass: 'uno-bg-xp-surface-0',
  },
  {
    step: '1',
    label: 'Section canvas',
    description: 'Default dark section for high-impact brand moments.',
    tokenClass: 'uno-bg-xp-surface-1',
  },
  {
    step: '2',
    label: 'Raised surface',
    description: 'Cards, callouts, and proof blocks above a section.',
    tokenClass: 'uno-bg-xp-surface-2',
  },
  {
    step: '3',
    label: 'Active surface',
    description: 'Interactive, selected, or highest local emphasis.',
    tokenClass: 'uno-bg-xp-surface-3',
  },
];

const typography: TypeSample[] = [
  {
    label: 'Display XL',
    size: 'display-xl',
    sample: 'A mature hypervisor for infrastructure that cannot fail.',
  },
  {
    label: 'Display L',
    size: 'display-l',
    sample: 'Layered virtualization, built in the open.',
  },
  {
    label: 'Heading XL',
    size: 'heading-xl',
    sample: 'Systems confidence',
  },
  {
    label: 'Heading L',
    size: 'heading-l',
    sample: 'A foundation for secure platforms',
  },
  {
    label: 'Heading M',
    size: 'heading-m',
    sample: 'Isolation layer',
  },
];

const layouts: TextCard[] = [
  {
    title: 'Container',
    description: 'Constrain reading width with xp-content, page layouts with xp-page, and broad compositions with xp-wide.',
  },
  {
    title: 'Section',
    description: 'Use full-width bands on surface 0 or 1. Sections should not need decorative wrappers.',
  },
  {
    title: 'Stack',
    description: 'Use vertical rhythm for prose, forms, and ordered content.',
  },
  {
    title: 'Flow',
    description: 'Let related text and actions follow source order with predictable gaps.',
  },
  {
    title: 'Grid',
    description: 'Use equal-weight proof, feature, member, and resource collections.',
  },
  {
    title: 'Split',
    description: 'Pair an argument with evidence, media, or a compact technical panel.',
  },
  {
    title: 'Cluster',
    description: 'Wrap badges, actions, filters, and metadata without layout shifts.',
  },
];

const primitives: TextCard[] = [
  {
    title: 'Button',
    description: 'Primary, secondary, and text actions. Keep labels short and states obvious.',
  },
  {
    title: 'Card',
    description: 'A repeated item on surface 2. Use for proof, resources, features, and links.',
  },
  {
    title: 'Badge',
    description: 'Compact status, category, release, or ecosystem labels.',
  },
  {
    title: 'Divider',
    description: 'A structural line that separates layers or content groups.',
  },
  {
    title: 'Surface',
    description: 'A token-backed background step: 0, 1, 2, 3, or intentional light.',
  },
  {
    title: 'Heading',
    description: 'Semantic heading with display and content sizes.',
  },
  {
    title: 'Text',
    description: 'Body, lead, caption, eyebrow, and monospaced annotations.',
  },
];

const iconography: TextCard[] = [
  {
    title: 'Size',
    description: 'Use 20px for inline UI, 24px for compact cards, and 32px only for larger explanatory moments.',
  },
  {
    title: 'Style',
    description: 'Prefer simple line icons with a consistent stroke. Filled icons are reserved for status or brand marks.',
  },
  {
    title: 'Alignment',
    description: 'Align icons to text cap height or center them in fixed-size controls to prevent layout shift.',
  },
  {
    title: 'Purpose',
    description: 'Use icons to clarify actions, statuses, systems, or navigation. Do not use them as decoration.',
  },
];

const accessibility: TextCard[] = [
  {
    title: 'Focus',
    description: 'Keyboard focus uses a visible token-backed ring on dark and light surfaces.',
  },
  {
    title: 'Keyboard',
    description: 'Interactive primitives must be reachable and usable without pointer input.',
  },
  {
    title: 'Contrast',
    description: 'Use documented text and surface pairings; do not invent opacity-based text tones.',
  },
  {
    title: 'Reduced motion',
    description: 'Motion should have a reduced-motion fallback and never hide content.',
  },
];

const motion: MotionSample[] = [
  {
    name: 'Fast',
    duration: '120ms',
    usage: 'Hover color, focus affordance, compact state change.',
  },
  {
    name: 'Base',
    duration: '200ms',
    usage: 'Elevation, small transform, disclosure.',
  },
  {
    name: 'Slow',
    duration: '360ms',
    usage: 'Section reveal or larger layered transition.',
  },
];

const foundationDetails: TextCard[] = [
  {
    title: 'Color',
    description: 'Use surface, text, border, accent, warning, and danger tokens as documented pairings.',
  },
  {
    title: 'Spacing',
    description: 'Use the 4px-based spacing scale and section rhythm. Dense technical content should remain scannable.',
  },
  {
    title: 'Elevation',
    description: 'Use elevation to show layer order, not to create decorative depth.',
  },
  {
    title: 'Motion',
    description: 'Motion clarifies relationship, emphasis, or state. It should be short, reversible, and optional.',
  },
];

const futureIllustrationScenes: TextCard[] = [
  {
    title: 'Safety boundary',
    description: 'Could extend the current layer language to explain isolation, containment, and assurance when a real page needs that story.',
  },
  {
    title: 'Automotive stack',
    description: 'Could adapt platform layers to show hardware domains, specialized guests, and mixed-criticality responsibilities.',
  },
  {
    title: 'Community network',
    description: 'Could use related visual principles to clarify maintainers, contributors, members, projects, and governance relationships.',
  },
  {
    title: 'Downloads flow',
    description: 'Could turn release artifacts, verification, documentation, and upgrade paths into an inspectable evaluation flow.',
  },
];

const diagramArrangements: ListCard[] = [
  {
    title: 'Separated layers',
    description: 'Use when the page needs to teach how a system is constructed. Physical separation helps readers see architecture, ownership, isolation, explicit boundaries, and layered responsibilities.',
    items: ['Architecture teaching', 'Ownership boundaries', 'Isolation stories', 'Layered responsibilities'],
  },
  {
    title: 'Compact stack',
    description: 'Use when the page needs to communicate the complete platform. Compact composition emphasizes integrated systems, deployment views, ecosystem relationships, and reference architectures.',
    items: ['Composed platforms', 'Integrated systems', 'Deployment views', 'Ecosystem relationships'],
  },
];

const layerSemantics: ListCard[] = [
  {
    title: 'Typical stack',
    description: 'Each layer should represent a meaningful architectural boundary.',
    items: ['Applications', 'Guest systems', 'Xen Hypervisor', 'Hardware'],
  },
  {
    title: 'Authoring rule',
    description: 'Labels may change for a domain, but the mental model should stay explicit.',
    items: ['Avoid decorative layers', 'Avoid non-architectural layers', 'Preserve boundaries and ownership'],
  },
];

const diagramMotionGuidance: ListCard[] = [
  {
    title: 'Preferred',
    description: 'Motion should communicate engineering, not entertainment.',
    items: ['Slight lift', 'Subtle brightening', 'Restrained depth', 'Gentle breathing', 'Reduced-motion fallback'],
  },
  {
    title: 'Avoid',
    description: 'Movement should never compete with technical content.',
    items: ['Bouncing', 'Spinning', 'Elastic easing', 'Continuous attention-grabbing loops', 'Distracting motion'],
  },
];

const designFeeling: ListCard[] = [
  {
    title: 'Should feel',
    description: 'Premium but restrained, with technical confidence rather than spectacle.',
    items: ['technical', 'calm', 'inspectable', 'precise', 'open', 'infrastructure-grade'],
  },
  {
    title: 'Avoid',
    description: 'Anything that makes Xen sound generic, magical, or decorative.',
    items: ['hype-first marketing', 'generic SaaS language', 'cyberpunk decoration', 'excessive neon', 'novelty motion', 'unsupported superiority claims'],
  },
];

const vocabularyGuidance: ListCard[] = [
  {
    title: 'Prefer',
    description: 'Use words because they clarify architecture, not because they sound technical. These are examples, not required terms.',
    items: ['explicit', 'visible', 'inspectable', 'boundaries', 'ownership', 'isolation', 'architecture', 'evidence', 'maintainable', 'long-lived', 'platform', 'deterministic', 'mixed-criticality', 'governance', 'participation', 'open engineering', 'certification-oriented', 'safety-conscious'],
  },
  {
    title: 'Avoid',
    description: 'Avoid vague marketing language when it hides the engineering claim. Context matters; this is judgment, not a banned-word list.',
    items: ['magic', 'seamless', 'effortless', 'revolutionary', 'next-generation', 'futuristic', 'best-in-class', 'unsupported enterprise-grade claims', 'generic cloud-native wording', 'generic cyber language'],
  },
];

const solutionPageArc = ['Hero and primary idea', 'Problem framing', 'Engineering principle', 'Technical capabilities', 'Domain example or process', 'Evaluation path', 'Governance and health', 'Participation', 'Final CTA'];

const pageQuestions: TextCard[] = [
  {
    title: 'Homepage',
    description: 'What is Xen, and why should engineers and organizations trust the project?',
  },
  {
    title: 'Embedded & Automotive',
    description: 'How can teams consolidate mixed-criticality workloads without blurring boundaries?',
  },
  {
    title: 'Cloud & Infrastructure',
    description: 'Why should infrastructure ownership remain explicit and inspectable?',
  },
  {
    title: 'Safety-Critical Systems',
    description: 'What does certification-oriented engineering require beyond architecture?',
  },
];

const memorableSentences = [
  'Safety work is evidence work.',
  'The architecture creates the boundary. The engineering process creates the evidence.',
  'Open infrastructure starts with an open virtualization layer.',
  'Consolidate the platform without blurring the boundaries.',
  'A project you can evaluate in the open.',
];

const signatureSections: TextCard[] = [
  {
    title: 'Homepage',
    description: 'Layered Xen architecture hero.',
  },
  {
    title: 'Embedded & Automotive',
    description: 'Mixed-criticality and platform composition story.',
  },
  {
    title: 'Cloud & Infrastructure',
    description: 'Xen-centered infrastructure ecosystem.',
  },
  {
    title: 'Safety-Critical Systems',
    description: 'Safety Committee and evidence trail.',
  },
];

const headingGuidance: ListCard[] = [
  {
    title: 'Eyebrows',
    description: 'Short category labels, usually styled uppercase by the system.',
    items: ['Use noun phrases', 'Orient the reader', 'Do not repeat the heading'],
  },
  {
    title: 'Headings',
    description: 'One main idea per heading, sentence case in authored text.',
    items: ['Keep concise', 'Use active language where possible', 'Avoid four-line wraps at ordinary desktop widths'],
  },
  {
    title: 'Introductions',
    description: 'Usually one short paragraph explaining why the section matters.',
    items: ['Do not restate every card', 'Avoid front-loaded caveats unless accuracy requires them', 'Connect the section to the page question'],
  },
];

const ctaGuidance: ListCard[] = [
  {
    title: 'Prefer',
    description: 'CTA labels should describe the destination or action.',
    items: ['Explore the hypervisor', 'Explore architecture', 'Review isolation resources', 'Explore XCP-ng', 'View CI resources', 'View downloads', 'Read governance', 'Review security policy', 'Start contributing', 'Become a member', 'Explore membership', 'View reference architecture'],
  },
  {
    title: 'Avoid',
    description: 'Vague labels make repeated cards harder to scan.',
    items: ['Learn more', 'Click here', 'Find out more', 'Open page', 'Repeated Explore labels without an object'],
  },
];

const diagramApi = [
  'title identifies the diagram; metaLabel is optional.',
  'summary is required and becomes the accessible description.',
  'variant supports standard and hero.',
  'bleed, frame sizing, overlap, shadows, item layout, and motion are derived internally.',
  'layers accept title, eyebrow, tone, emphasis, description, and items.',
  'layer emphasis currently supports primary.',
  'layer tones currently support applications, guests, xen, and hardware.',
];

const layerCountGuidance: ListCard[] = [
  {
    title: 'Proven counts',
    description: 'Current solution pages demonstrate variable layer counts.',
    items: ['4-layer Embedded & Automotive', '4-layer Cloud & Infrastructure', '5-layer Safety-Critical Systems'],
  },
  {
    title: 'Authoring range',
    description: 'Use enough layers to teach the architecture, not to decorate the frame.',
    items: ['Aim for 3-6 meaningful layers', 'Map each layer to a real concept', 'Use another format if the story needs many more layers'],
  },
];

const diagramRules: ListCard[] = [
  {
    title: 'Xen emphasis',
    description: 'When Xen appears, the Xen boundary should generally be the visual anchor.',
    items: ['Use emphasis: primary', 'Keep the glow restrained', 'Do not add page-local glow CSS', 'Keep other layers readable'],
  },
  {
    title: 'Frame bleed',
    description: 'Subtle inline-start and bottom bleed is part of the shared visual language.',
    items: ['Derived internally', 'Stronger for hero', 'Restrained for standard', 'Never clip meaningful content'],
  },
  {
    title: 'Protected content',
    description: 'Overlap belongs in quiet plate margins, never across meaningful copy or item labels.',
    items: ['Content-driven frame height', 'Density-aware layer step', 'Five-layer stacks get more space', 'No page-specific offsets'],
  },
  {
    title: 'Content',
    description: 'The diagram teaches structure; surrounding prose explains details.',
    items: ['Short eyebrow', 'Clear title', 'Brief description', 'Up to 4 items', 'Four items form a 2-by-2 layout', 'Avoid paragraphs on plates'],
  },
  {
    title: 'Accessibility',
    description: 'The page must remain understandable without the visual geometry.',
    items: ['Match visual order to semantic order', 'Write a concise summary', 'Hide decorative geometry', 'Do not require hover or motion'],
  },
];

const deferredDiagramFeatures = ['connectors', 'overlays', 'annotations', 'generic badges', 'arbitrary per-layer geometry', 'universal diagram plugins', 'interactive diagram builders'];

const blockSelectionGuidance: ListCard[] = [
  {
    title: 'FeatureGrid',
    description: 'Use for similarly weighted items.',
    items: ['Parallel capabilities', 'Evaluation criteria', 'Related resources', 'Project-health signals', 'Avoid for sequences or signature sections'],
  },
  {
    title: 'Local Grid + Card',
    description: 'Use when the content structure belongs to one narrative.',
    items: ['Different card roles', 'Distinct hierarchy', 'Unique layout', 'Sections that break normal rhythm'],
  },
  {
    title: 'Callout',
    description: 'Use for bounded context that needs more weight than body copy.',
    items: ['Engineering statement', 'Caution or scope clarification', 'Reference architecture context', 'Participation note', 'Evidence/process clarification'],
  },
  {
    title: 'CTA',
    description: 'Use for a genuine next step, not to fill space.',
    items: ['End-of-page action', 'Focused mid-page decision point', 'Participation or evaluation path'],
  },
  {
    title: 'Stats and LogoCloud',
    description: 'Use only where the proof is meaningful and sourced.',
    items: ['Stats for quantitative context', 'LogoCloud for member or ecosystem trust', 'Do not imply endorsement beyond known relationships'],
  },
];

const recurringPatterns: ListCard[] = [
  {
    title: 'Related resources',
    description: 'Help evaluators continue from architecture to docs, downloads, governance, or contribution.',
    items: ['Use FeatureGrid when links are equal weight', 'Avoid if the links duplicate primary navigation'],
  },
  {
    title: 'Open governance / project health',
    description: 'Show why the project can be inspected and sustained.',
    items: ['Governance', 'Security process', 'CI', 'Releases', 'Contribution paths'],
  },
  {
    title: 'Ecosystem roles',
    description: 'Explain how Xen complements surrounding projects and tooling.',
    items: ['Prefer local Grid + Card when roles differ', 'Avoid implying replacement or endorsement'],
  },
  {
    title: 'Participation / membership',
    description: 'Connect engineering work to public contribution and member support.',
    items: ['Use Callout or CTA', 'Use participation language instead of sales language'],
  },
  {
    title: 'Technical capabilities',
    description: 'Present mechanisms after the engineering problem is clear.',
    items: ['FeatureGrid works for parallel capabilities', 'Capabilities should support the primary answer'],
  },
  {
    title: 'Evaluation checklist',
    description: 'Turn architecture into practical review questions.',
    items: ['FeatureGrid works for equal criteria', 'Avoid exhaustive feature catalogs'],
  },
  {
    title: 'Engineering caution note',
    description: 'Clarify scope near the relevant technical claim.',
    items: ['Use Callout or compact Surface', 'Be educational and specific', 'Say what Xen supports, not only what it does not'],
  },
];

const blockActions = [
  {label: 'Primary action', href: '#blocks'},
  {label: 'Secondary action', href: '#composition', variant: 'secondary' as const},
];

const blockFeatures = [
  {
    icon: 'i-carbon-layers',
    title: 'Layered systems',
    description: 'Use cards for reusable proof, capabilities, and technical explanations.',
    href: '#composition',
    linkLabel: 'View composition',
  },
  {
    icon: 'i-carbon-security',
    title: 'Trust signals',
    description: 'Pair claims with evidence that future pages can reuse without custom layout code.',
  },
  {
    icon: 'i-carbon-ibm-cloud-vpc',
    title: 'Infrastructure fit',
    description: 'Keep dense technical content scannable across small and large screens.',
  },
];

const blockStats = [
  {
    value: '20+ years',
    label: 'Open development',
    description: 'A long-running project needs proof blocks that feel mature, not nostalgic.',
    sourceHref: 'https://xenproject.org/',
    sourceLabel: 'Project site',
  },
  {
    value: '3 layers',
    label: 'Reusable hierarchy',
    description: 'Primitives, blocks, and pages keep layout decisions in the right place.',
  },
  {
    value: '0 legacy CSS',
    label: 'Clean boundary',
    description: 'New blocks stay independent of the migrated Sass cascade.',
  },
];
---

<BaseLayout title="Design System" description="Internal Xen Project design system reference." robots="noindex, nofollow" bodyClass="xp-design-system">
  <main class="uno-min-h-screen uno-bg-xp-surface-0 uno-text-xp-text-primary">
    <div class="uno-px-4 uno-pt-4 md:uno-px-6">
      <Container>
        <Surface as="aside" tone="1" padding="sm" class="uno-bg-[linear-gradient(90deg,rgb(133_194_65_/_0.10),transparent_26rem)]" aria-label="Internal development resource">
          <Cluster align="start">
            <Text as="span" size="caption" tone="accent-primary" class="uno-shrink-0 uno-font-bold">Internal development resource</Text>
            <Text size="caption">
              This page documents the Xen Design System and is intended for contributors and maintainers. It is not part of the public website.
            </Text>
          </Cluster>
        </Surface>
      </Container>
    </div>

    <Section variant="hero">
      <Container>
        <Split align="center">
          <Stack gap="5" class="uno-max-w-xp-page">
            <Text size="eyebrow" tone="accent-primary">Xen Design System</Text>
            <Heading as="h1" size="display-xl" class="uno-max-w-5xl">Dark layered foundations for infrastructure-grade pages.</Heading>
            <Text size="lead" class="uno-max-w-xp-content">
              This page is the source of truth for new XenProject.org pages. It keeps the system small: principles, tokens, layout rules, primitives, accessibility, and a few compositions.
            </Text>
          </Stack>
          <LayerStackDemo />
        </Split>
      </Container>
    </Section>

    <Section>
      <Container>
        <Stack gap="6">
          <Stack gap="3" class="uno-max-w-xp-content">
            <Text size="eyebrow" tone="muted">Principles</Text>
            <Heading size="heading-xl">Engineering communication before marketing polish.</Heading>
            <Text>
              The Xen Project design system is an engineering communication system. Components and visual patterns exist to make architecture, isolation, ownership, evidence, governance, and participation easier to understand. Pages should teach before they promote.
            </Text>
          </Stack>
          <Grid columns="3">
            {principles.map(principle => (
              <Card>
                <Heading as="h3" size="heading-m">{principle.title}</Heading>
                <Text size="caption" class="uno-mt-3">{principle.description}</Text>
              </Card>
            ))}
          </Grid>
          <Grid columns="2">
            {designFeeling.map(rule => (
              <Card>
                <Heading as="h3" size="heading-m">{rule.title}</Heading>
                <Text size="caption" class="uno-mt-2">{rule.description}</Text>
                <Cluster gap="2" class="uno-mt-4">
                  {rule.items.map(item => <Badge variant={rule.title === 'Should feel' ? 'info' : 'muted'}>{item}</Badge>)}
                </Cluster>
              </Card>
            ))}
          </Grid>
        </Stack>
      </Container>
    </Section>

    <Section surface="1">
      <Container>
        <Stack gap="6">
          <Stack gap="3" class="uno-max-w-xp-content">
            <Text size="eyebrow" tone="muted">Foundations</Text>
            <Heading size="heading-xl">Surfaces carry the identity.</Heading>
            <Text>
              Dark surfaces are the brand canvas. Light surfaces are secondary tools for readability, data density, or intentional contrast.
            </Text>
          </Stack>
          <Grid columns="4">
            {surfaces.map(surface => (
              <TokenSwatch step={surface.step} label={surface.label} description={surface.description} tokenClass={surface.tokenClass} />
            ))}
          </Grid>
          <Grid columns="2">
            {foundationDetails.map(detail => (
              <Stack gap="3">
                <Divider />
                <Heading as="h3" size="heading-m">{detail.title}</Heading>
                <Text size="caption">{detail.description}</Text>
              </Stack>
            ))}
          </Grid>
        </Stack>
      </Container>
    </Section>

    <Section>
      <Container>
        <Stack gap="6">
          <Stack gap="3" class="uno-max-w-xp-content">
            <Text size="eyebrow" tone="muted">Typography</Text>
            <Heading size="heading-xl">A calm technical scale.</Heading>
            <Text>Inter carries the main voice. JetBrains Mono is reserved for version labels, code-like metadata, and technical annotations.</Text>
          </Stack>
          <Stack gap="5">
            {typography.map(typeSample => (
              <div class="uno-grid uno-grid-cols-1 uno-gap-2 uno-border-0 uno-border-t uno-border-solid uno-border-xp-border-muted uno-pt-5 md:uno-grid-cols-[minmax(7rem,0.22fr)_minmax(0,1fr)] md:uno-gap-5">
                <Text size="mono-small" tone="muted">{typeSample.label}</Text>
                <Heading as="h3" size={typeSample.size}>{typeSample.sample}</Heading>
              </div>
            ))}
            <div class="uno-grid uno-grid-cols-1 uno-gap-2 uno-border-0 uno-border-t uno-border-solid uno-border-xp-border-muted uno-pt-5 md:uno-grid-cols-[minmax(7rem,0.22fr)_minmax(0,1fr)] md:uno-gap-5">
              <Text size="mono-small" tone="muted">Body</Text>
              <Text>Body copy explains the claim with enough technical specificity to build confidence without adding visual noise.</Text>
            </div>
            <div class="uno-grid uno-grid-cols-1 uno-gap-2 uno-border-0 uno-border-t uno-border-solid uno-border-xp-border-muted uno-pt-5 md:uno-grid-cols-[minmax(7rem,0.22fr)_minmax(0,1fr)] md:uno-gap-5">
              <Text size="mono-small" tone="muted">Caption</Text>
              <Text size="caption" tone="muted">Caption text supports metadata, labels, timestamps, and lower-emphasis explanatory copy.</Text>
            </div>
          </Stack>
        </Stack>
      </Container>
    </Section>

    <Section surface="1">
      <Container>
        <Stack gap="6">
          <Stack gap="3" class="uno-max-w-xp-content">
            <Text size="eyebrow" tone="muted">Layout</Text>
            <Heading size="heading-xl">Seven layout primitives are enough.</Heading>
            <Text>Pages should be composed from a small set of repeatable layout behaviors before introducing named blocks.</Text>
          </Stack>
          <Grid columns="2">
            {layouts.map(layout => (
              <Card>
                <Heading as="h3" size="heading-m">{layout.title}</Heading>
                <Text size="caption" class="uno-mt-2">{layout.description}</Text>
              </Card>
            ))}
          </Grid>
        </Stack>
      </Container>
    </Section>

    <Section>
      <Container>
        <Stack gap="6">
          <Stack gap="3" class="uno-max-w-xp-content">
            <Text size="eyebrow" tone="muted">Primitives</Text>
            <Heading size="heading-xl">Reusable before expressive.</Heading>
            <Text>Primitives own semantics, variants, and state. Shortcuts should only capture shared visual recipes.</Text>
          </Stack>
          <Split align="center">
            <Stack gap="5">
              <Cluster>
                <Button href="#composition">Primary action</Button>
                <Button href="#accessibility" variant="secondary">Secondary action</Button>
                <Button href="#motion" variant="text">Text link</Button>
              </Cluster>
              <Cluster>
                <Badge>Stable</Badge>
                <Badge variant="info">Open source</Badge>
                <Badge variant="muted">Hypervisor</Badge>
              </Cluster>
            </Stack>
            <Grid columns="2">
              {primitives.map(primitive => (
                <Card>
                  <Heading as="h3" size="heading-m">{primitive.title}</Heading>
                  <Text size="caption" class="uno-mt-2">{primitive.description}</Text>
                </Card>
              ))}
            </Grid>
          </Split>
        </Stack>
      </Container>
    </Section>

    <Section surface="1">
      <Container>
        <Stack gap="6">
          <Stack gap="3" class="uno-max-w-xp-content">
            <Text size="eyebrow" tone="muted">Iconography</Text>
            <Heading size="heading-xl">Icons support technical clarity.</Heading>
            <Text>Use icons sparingly and consistently. They should identify system concepts, actions, status, or navigation faster than text alone.</Text>
          </Stack>
          <Split align="center">
            <IconSamples />
            <Grid columns="2">
              {iconography.map(iconRule => (
                <Card>
                  <Heading as="h3" size="heading-m">{iconRule.title}</Heading>
                  <Text size="caption" class="uno-mt-2">{iconRule.description}</Text>
                </Card>
              ))}
            </Grid>
          </Split>
        </Stack>
      </Container>
    </Section>

    <Section id="accessibility" surface="1">
      <Container>
        <Stack gap="6">
          <Stack gap="3" class="uno-max-w-xp-content">
            <Text size="eyebrow" tone="muted">Accessibility</Text>
            <Heading size="heading-xl">Accessibility is part of the visual system.</Heading>
            <Text>New primitives and blocks must preserve focus, keyboard operation, contrast, and reduced-motion behavior by default.</Text>
          </Stack>
          <Grid columns="2">
            {accessibility.map(accessibilityRule => (
              <Card>
                <Heading as="h3" size="heading-m">{accessibilityRule.title}</Heading>
                <Text size="caption" class="uno-mt-2">{accessibilityRule.description}</Text>
              </Card>
            ))}
          </Grid>
        </Stack>
      </Container>
    </Section>

    <Section id="motion">
      <Container>
        <Stack gap="6">
          <Stack gap="3" class="uno-max-w-xp-content">
            <Text size="eyebrow" tone="muted">Motion</Text>
            <Heading size="heading-xl">Subtle movement, clear purpose.</Heading>
            <Text>Use standard easing for most states and emphasized easing only when a layer changes position or prominence.</Text>
          </Stack>
          <Grid columns="3">
            {motion.map(motionSample => (
              <MotionDemoCard name={motionSample.name} duration={motionSample.duration} usage={motionSample.usage} />
            ))}
          </Grid>
        </Stack>
      </Container>
    </Section>

    <Section id="illustrations" surface="1">
      <Container>
        <Stack gap="6">
          <Stack gap="3" class="uno-max-w-xp-content">
            <Text size="eyebrow" tone="muted">Illustration system</Text>
            <Heading size="heading-xl">Layered architecture is the primary visual language.</Heading>
            <Text>
              The Xen Project visual identity is built around layered platform architecture. Layer diagrams are not illustrations added after page design. They are first-class design components that communicate architecture, isolation, ownership, explicit boundaries, and inspectable systems.
            </Text>
            <Text>
              Future solution pages should start from this language when it clarifies the engineering story, while allowing new arrangements to emerge from real page needs.
            </Text>
          </Stack>
          <Split align="center">
            <LayeredPlatform id="internal-layered-platform" variant="compact" motion="none" />
            <Stack gap="4">
              <Heading as="h3" size="heading-m">{layeredPlatformScene.title}</Heading>
              <Text size="caption">{layeredPlatformScene.description}</Text>
              <Grid columns="2">
                {layeredPlatformScene.assetPlan.map(asset => (
                  <Surface tone="2" border="muted" padding="md" radius="md" shadow="sm">
                    <Text size="mono-small" tone="accent-primary" class="uno-font-semibold">{asset.complexity}</Text>
                    <Heading as="h4" size="heading-m" class="uno-mt-2">{asset.id}</Heading>
                    <Text size="caption" class="uno-mt-2">{asset.purpose}</Text>
                  </Surface>
                ))}
              </Grid>
            </Stack>
          </Split>
          <Surface tone="2" border="strong" padding="lg" radius="lg" shadow="sm">
            <Stack gap="4">
              <Stack gap="2" class="uno-max-w-xp-content">
                <Heading as="h3" size="heading-m">Layered diagram authoring contract</Heading>
                <Text size="caption">
                  The reusable component is <code>PlatformLayersDiagram</code>. Each plate represents a meaningful system boundary, responsibility, or layer. The diagram is not decorative artwork.
                </Text>
              </Stack>
              <Grid columns="2">
                {diagramApi.map(item => (
                  <Surface tone="3" border="muted" padding="md" radius="md" shadow="sm">
                    <Text size="caption">{item}</Text>
                  </Surface>
                ))}
              </Grid>
            </Stack>
          </Surface>
          <Grid columns="2">
            {diagramArrangements.map(state => (
              <Card>
                <Text size="mono-small" tone="accent-primary" class="uno-font-semibold">Common arrangement</Text>
                <Heading as="h3" size="heading-m" class="uno-mt-2">{state.title}</Heading>
                <Text size="caption" class="uno-mt-2">{state.description}</Text>
                <ul class="uno-mt-4 uno-grid uno-gap-2 uno-pl-4 uno-text-sm uno-text-xp-text-muted">
                  {state.items.map(item => <li>{item}</li>)}
                </ul>
              </Card>
            ))}
          </Grid>
          <Text size="caption" tone="muted" class="uno-max-w-xp-content">
            Neither arrangement is better. Choose the one that best supports the engineering story, and do not freeze the visual language around only these two forms.
          </Text>
          <Grid columns="2">
            {layerSemantics.map(rule => (
              <Surface tone="2" border="muted" padding="md" radius="md" shadow="sm">
                <Heading as="h3" size="heading-m">{rule.title}</Heading>
                <Text size="caption" class="uno-mt-2">{rule.description}</Text>
                <Cluster gap="2" class="uno-mt-4">
                  {rule.items.map(item => <Badge variant="muted">{item}</Badge>)}
                </Cluster>
              </Surface>
            ))}
          </Grid>
          <Grid columns="2">
            {layerCountGuidance.map(rule => (
              <Card>
                <Heading as="h3" size="heading-m">{rule.title}</Heading>
                <Text size="caption" class="uno-mt-2">{rule.description}</Text>
                <Cluster gap="2" class="uno-mt-4">
                  {rule.items.map(item => <Badge variant="muted">{item}</Badge>)}
                </Cluster>
              </Card>
            ))}
          </Grid>
          <Grid columns="2">
            <Stack gap="3">
              <Text size="mono-small" tone="accent-primary" class="uno-font-semibold">4-layer example</Text>
              <PlatformLayersDiagram id="internal-diagram-four-layer" {...embeddedAutomotivePlatformLayersDiagram} metaLabel={undefined} />
            </Stack>
            <Stack gap="3">
              <Text size="mono-small" tone="accent-primary" class="uno-font-semibold">5-layer example with subtle bleed</Text>
              <PlatformLayersDiagram id="internal-diagram-five-layer" {...safetyCriticalPlatformLayersDiagram} />
            </Stack>
          </Grid>
          <Grid columns="2">
            {diagramRules.map(rule => (
              <Card>
                <Heading as="h3" size="heading-m">{rule.title}</Heading>
                <Text size="caption" class="uno-mt-2">{rule.description}</Text>
                <ul class="uno-mt-4 uno-grid uno-gap-2 uno-pl-4 uno-text-sm uno-text-xp-text-muted">
                  {rule.items.map(item => <li>{item}</li>)}
                </ul>
              </Card>
            ))}
          </Grid>
          <Grid columns="2">
            {diagramMotionGuidance.map(rule => (
              <Stack gap="3">
                <Divider />
                <Heading as="h3" size="heading-m">{rule.title}</Heading>
                <Text size="caption">{rule.description}</Text>
                <Text size="caption" tone="muted">{rule.items.join(' · ')}</Text>
              </Stack>
            ))}
          </Grid>
          <Surface tone="2" border="muted" padding="lg" radius="md" shadow="sm">
            <Stack gap="3">
              <Heading as="h3" size="heading-m">Not yet part of the system</Heading>
              <Text size="caption">
                Do not extend the diagram API speculatively. Add new capabilities only when a real page demonstrates that the current language cannot communicate its story clearly.
              </Text>
              <Cluster gap="2">
                {deferredDiagramFeatures.map(feature => <Badge variant="muted">{feature}</Badge>)}
              </Cluster>
            </Stack>
          </Surface>
          <Stack gap="2" class="uno-max-w-xp-content">
            <Heading as="h3" size="heading-m">Future applications (illustrative)</Heading>
            <Text size="caption">
              These examples show where the existing visual language could naturally extend if future pages require it. They are not approved components, planned APIs, or implementation commitments.
            </Text>
          </Stack>
          <Grid columns="4">
            {futureIllustrationScenes.map(scene => (
              <Card>
                <Text size="mono-small" tone="muted" class="uno-font-semibold">Illustrative</Text>
                <Heading as="h3" size="heading-m" class="uno-mt-2">{scene.title}</Heading>
                <Text size="caption" class="uno-mt-2">{scene.description}</Text>
              </Card>
            ))}
          </Grid>
        </Stack>
      </Container>
    </Section>

    <Section id="experience-guidance">
      <Container>
        <Stack gap="6">
          <Stack gap="3" class="uno-max-w-xp-content">
            <Text size="eyebrow" tone="muted">Experience guidance</Text>
            <Heading size="heading-xl">Pages should feel related, not templated.</Heading>
            <Text>
              Solution pages should teach the system before marketing the outcome. Use the same architectural vocabulary and narrative rhythm, then adapt the emphasis to the domain.
            </Text>
          </Stack>
          <Surface tone="2" border="muted" padding="lg" radius="md" shadow="sm">
            <Stack gap="4">
              <Stack gap="2" class="uno-max-w-xp-content">
                <Text size="mono-small" tone="accent-primary" class="uno-font-semibold">Architecture-first storytelling</Text>
                <Heading as="h3" size="heading-m">Solution-page arc</Heading>
                <Text size="caption">
                  Teach architecture before features. Explain the engineering problem before presenting capabilities. Show how Xen fits into a system before asking the reader to participate.
                </Text>
              </Stack>
              <Cluster gap="2">
                {solutionPageArc.map(step => <Badge>{step}</Badge>)}
              </Cluster>
              <Text size="caption" tone="muted">
                This is a starting point, not a rigid template. Section order should follow the narrative, and pages should not include sections merely because another page has them.
              </Text>
            </Stack>
          </Surface>
          <Grid columns="2">
            <Surface tone="2" border="muted" padding="lg" radius="md" shadow="sm">
              <Stack gap="4">
                <Stack gap="2">
                  <Heading as="h3" size="heading-m">Primary Question</Heading>
                  <Text size="caption">
                    Every major page should answer one primary engineering question. If the page cannot be summarized as a question, its narrative may be too broad.
                  </Text>
                </Stack>
                {pageQuestions.map(question => (
                  <Stack gap="1">
                    <Text size="mono-small" tone="accent-primary" class="uno-font-semibold">{question.title}</Text>
                    <Text size="caption">{question.description}</Text>
                  </Stack>
                ))}
              </Stack>
            </Surface>
            <Surface tone="2" border="muted" padding="lg" radius="md" shadow="sm">
              <Stack gap="4">
                <Stack gap="2">
                  <Heading as="h3" size="heading-m">One Memorable Sentence</Heading>
                  <Text size="caption">
                    Use one concise sentence that expresses the page's point of view. It should sound like engineering judgment, not a slogan, and it should not be repeated.
                  </Text>
                </Stack>
                {memorableSentences.map(sentence => (
                  <Text size="caption" tone="primary" class="uno-font-semibold">{sentence}</Text>
                ))}
              </Stack>
            </Surface>
          </Grid>
          <Surface tone="2" border="strong" padding="lg" radius="lg" shadow="sm">
            <Stack gap="4">
              <Stack gap="2" class="uno-max-w-xp-content">
                <Heading as="h3" size="heading-m">One signature section per major page</Heading>
                <Text size="caption">
                  Each solution page should contain one memorable section that breaks the normal page rhythm and belongs specifically to that subject. This prevents repeated card grids and gives readers one visual or editorial moment to remember.
                </Text>
              </Stack>
              <Grid columns="4">
                {signatureSections.map(section => (
                  <Card>
                    <Heading as="h4" size="heading-m">{section.title}</Heading>
                    <Text size="caption" class="uno-mt-2">{section.description}</Text>
                  </Card>
                ))}
              </Grid>
            </Stack>
          </Surface>
          <Text size="caption" tone="muted" class="uno-max-w-xp-content">
            Prefer language that makes engineering decisions, responsibilities, ownership, and evidence visible. The goal is editorial clarity, not technical-sounding density.
          </Text>
          <Grid columns="2">
            {vocabularyGuidance.map(rule => (
              <Card>
                <Heading as="h3" size="heading-m">{rule.title}</Heading>
                <Text size="caption" class="uno-mt-2">{rule.description}</Text>
                <Cluster gap="2" class="uno-mt-4">
                  {rule.items.map(item => <Badge variant={rule.title === 'Prefer' ? 'info' : 'muted'}>{item}</Badge>)}
                </Cluster>
              </Card>
            ))}
          </Grid>
          <Grid columns="3">
            {headingGuidance.map(rule => (
              <Card>
                <Heading as="h3" size="heading-m">{rule.title}</Heading>
                <Text size="caption" class="uno-mt-2">{rule.description}</Text>
                <ul class="uno-mt-4 uno-grid uno-gap-2 uno-pl-4 uno-text-sm uno-text-xp-text-muted">
                  {rule.items.map(item => <li>{item}</li>)}
                </ul>
              </Card>
            ))}
          </Grid>
          <Grid columns="2">
            {ctaGuidance.map(rule => (
              <Card>
                <Heading as="h3" size="heading-m">CTA labels: {rule.title}</Heading>
                <Text size="caption" class="uno-mt-2">{rule.description}</Text>
                <Cluster gap="2" class="uno-mt-4">
                  {rule.items.map(item => <Badge variant={rule.title === 'Prefer' ? 'info' : 'muted'}>{item}</Badge>)}
                </Cluster>
              </Card>
            ))}
          </Grid>
          <Callout
            title="Caution and qualification callouts"
            description="Use cautionary content when certification context needs clarification, a capability depends on system design, a project artifact is an input rather than a guarantee, or a reference architecture is illustrative. Keep the tone educational, concise, and specific."
            variant="warning"
          >
            <span slot="icon" class="i-carbon-warning-alt"></span>
          </Callout>
          <Surface tone="2" border="muted" padding="md" radius="md" shadow="sm">
            <Text size="caption" tone="primary" class="uno-font-semibold">
              Example: Xen source code is not itself safety certified. Certification depends on the complete system, integration context, process, evidence, tooling, validation, and safety case.
            </Text>
          </Surface>
        </Stack>
      </Container>
    </Section>

    <Section id="blocks" surface="1">
      <Container>
        <Stack gap="7">
          <Stack gap="3" class="uno-max-w-xp-content">
            <Text size="eyebrow" tone="muted">Blocks</Text>
            <Heading size="heading-xl">Reusable sections for high-impact pages.</Heading>
            <Text>
              Blocks compose primitives into repeatable content patterns. These examples show the component APIs without defining a future homepage.
            </Text>
          </Stack>
          <Grid columns="2">
            {blockSelectionGuidance.map(rule => (
              <Card>
                <Heading as="h3" size="heading-m">{rule.title}</Heading>
                <Text size="caption" class="uno-mt-2">{rule.description}</Text>
                <ul class="uno-mt-4 uno-grid uno-gap-2 uno-pl-4 uno-text-sm uno-text-xp-text-muted">
                  {rule.items.map(item => <li>{item}</li>)}
                </ul>
              </Card>
            ))}
          </Grid>
          <Surface tone="2" border="muted" padding="lg" radius="md" shadow="sm">
            <Stack gap="4">
              <Stack gap="2" class="uno-max-w-xp-content">
                <Heading as="h3" size="heading-m">Recurring patterns, no extraction yet</Heading>
                <Text size="caption">
                  These patterns are documented for authoring consistency. Do not create new shared components until real future pages prove a stable repeated need.
                </Text>
              </Stack>
              <Grid columns="3">
                {recurringPatterns.map(pattern => (
                  <Card>
                    <Heading as="h4" size="heading-m">{pattern.title}</Heading>
                    <Text size="caption" class="uno-mt-2">{pattern.description}</Text>
                    <Text size="caption" tone="muted" class="uno-mt-3">{pattern.items.join(' · ')}</Text>
                  </Card>
                ))}
              </Grid>
            </Stack>
          </Surface>
        </Stack>
      </Container>
    </Section>

    <Hero
      id="block-hero"
      eyebrow="Hero block"
      title="A reusable lead section for technical pages."
      description="The Hero block supports a focused argument, primary and secondary actions, optional media, and compact proof without relying on page-specific code."
      actions={blockActions}
      headingLevel="h2"
    >
      <Surface slot="media" tone="2" border="strong" radius="lg" shadow="md" padding="lg" aria-label="Layered infrastructure visual">
        <Stack gap="4">
          <Cluster justify="between">
            <Badge variant="accent">Surface 2</Badge>
            <Text as="span" size="mono-small" tone="muted">media slot</Text>
          </Cluster>
          <div class="uno-grid uno-gap-3">
            <div class="uno-h-16 uno-rounded-xp-md uno-border uno-border-solid uno-border-xp-border-muted uno-bg-[linear-gradient(90deg,rgb(133_194_65_/_0.18),transparent)]"></div>
            <div class="uno-ml-6 uno-h-16 uno-rounded-xp-md uno-border uno-border-solid uno-border-xp-border-muted uno-bg-xp-surface-3"></div>
            <div class="uno-ml-12 uno-h-16 uno-rounded-xp-md uno-border uno-border-solid uno-border-xp-border-muted uno-bg-xp-surface-2"></div>
          </div>
        </Stack>
      </Surface>
      <Surface slot="proof" tone="3" border="strong" padding="md" radius="md" shadow="sm">
        <Text size="caption" tone="accent-primary" class="uno-font-bold">Proof slot</Text>
        <Text size="caption" class="uno-mt-2">Use this area for credible metadata, not decoration.</Text>
      </Surface>
    </Hero>

    <LogoCloud
      id="block-logo-cloud"
      eyebrow="LogoCloud block"
      title="Credibility without carousel behavior."
      description="LogoCloud presents members, sponsors, users, or ecosystem marks in a static accessible grid."
      headingLevel="h2"
      surface="light"
      logos={memberLogos}
    />

    <FeatureGrid
      id="block-feature-grid"
      eyebrow="FeatureGrid block"
      title="Reusable cards for technical arguments."
      description="FeatureGrid is the default workhorse for capabilities, benefits, and use-case summaries."
      features={blockFeatures}
    />

    <Stats
      id="block-stats"
      eyebrow="Stats block"
      title="Proof that reads as content."
      description="Stats uses strong typography and optional source links without animated counters."
      stats={blockStats}
    />

    <CTA
      id="block-cta"
      eyebrow="CTA block"
      title="A standard next step for redesigned pages."
      description="CTA keeps action areas consistent while allowing dark-first and light contrast surfaces."
      actions={blockActions}
      align="center"
    />

    <Section id="block-callout" surface="0">
      <Container>
        <Stack gap="5">
          <Stack gap="3" class="uno-max-w-xp-content">
            <Text size="eyebrow" tone="muted">Callout block</Text>
            <Heading size="heading-xl">Compact highlights for important context.</Heading>
          </Stack>
          <Grid columns="2">
            <Callout
              title="Accent callout"
              description="Use this for release notes, sponsor moments, or page-specific emphasis that should remain bounded."
              action={{label: 'Read more', href: '#blocks', variant: 'text'}}
            >
              <span slot="icon" class="i-carbon-information"></span>
            </Callout>
            <Callout
              title="Warning callout"
              description="Important notices include a visible label so the message does not rely on color alone."
              variant="warning"
              action={{label: 'Review notice', href: '#accessibility', variant: 'secondary'}}
            >
              <span slot="icon" class="i-carbon-warning-alt"></span>
            </Callout>
          </Grid>
        </Stack>
      </Container>
    </Section>

    <Section id="composition" surface="1">
      <Container>
        <Stack gap="6">
          <Stack gap="3" class="uno-max-w-xp-content">
            <Text size="eyebrow" tone="muted">Composition</Text>
            <Heading size="heading-xl">Dark identity, intentional light rhythm.</Heading>
            <Text>
              This miniature composition shows how a page can move from dark canvas to raised dark panels, use one light contrast callout, then return to a dark action surface.
            </Text>
          </Stack>
          <DesignSystemExample>
            <Surface tone="1" padding="md">
              <Stack gap="4">
                <Stack gap="3">
                  <Text size="eyebrow" tone="accent-primary">Dark section</Text>
                  <Heading as="h3" size="heading-l">Layered infrastructure narrative.</Heading>
                </Stack>
                <Grid columns="2">
                  <Card>
                    <span class="i-carbon-layers uno-text-2xl uno-text-xp-accent-primary" aria-hidden="true"></span>
                    <Heading as="h4" size="heading-m" class="uno-mt-4">Isolation layer</Heading>
                    <Text size="caption" class="uno-mt-2">A raised card on surface 2 for reusable proof or feature content.</Text>
                  </Card>
                  <Card active>
                    <span class="i-carbon-security uno-text-2xl uno-text-xp-accent-primary" aria-hidden="true"></span>
                    <Heading as="h4" size="heading-m" class="uno-mt-4">Trust layer</Heading>
                    <Text size="caption" class="uno-mt-2">Surface 3 marks the active or locally emphasized panel.</Text>
                  </Card>
                </Grid>
                <Surface as="aside" tone="light-raised" border="light" shadow="md">
                  <Heading as="h4" size="heading-m" tone="on-light">Light contrast callout</Heading>
                  <Text size="caption" tone="on-light-muted" class="uno-mt-2">Use light surfaces for readability, dense data, or a deliberate rhythm change. Keep them bounded.</Text>
                </Surface>
                <Surface tone="3" border="strong">
                  <Cluster justify="between" gap="4">
                    <div>
                      <Heading as="h4" size="heading-m">Return to dark</Heading>
                      <Text size="caption" class="uno-mt-2">The composition ends on the primary brand canvas instead of drifting into a light theme.</Text>
                    </div>
                    <Button href="#top">Primary action</Button>
                  </Cluster>
                </Surface>
              </Stack>
            </Surface>
          </DesignSystemExample>
        </Stack>
      </Container>
    </Section>
  </main>
</BaseLayout>
