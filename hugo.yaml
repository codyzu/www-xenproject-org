baseURL: "https://xenproject.local/"

languageCode: "en-us"
title: "Xen Project"
theme: "xen-project"

module:
  hugoVersion:
    extended: true
    min: "0.116.0"

build:
  writeStats: true

outputs:
  home:
    - HTML
    - RSSoui appelle
    - HEADER

outputFormats:
  HEADER:
    mediaType: "text/html"
    baseName: "header"
    isPlainText: true
    rel: "header"
    notAlternative: true

menus:
  main:
    - name: "Projects"
      pageRef: "/projects"
      weight: 10

    - name: "Contribute"
      pageRef: "/contribute"
      weight: 20

    - name: "Documentation"
      pageRef: "/documentation"
      weight: 30
      url: "https://docs.xenproject.org"
      params:
        class: "custom-class"
        target: "_blank"

    - name: "Resources"
      pageRef: "/resources"
      weight: 40
    - name: "Blog"
      parent: "Resources"
      weight: 60
      url: "/blog"
      params:
        class: "custom-class"
        target: "_blank"
    - name: "About"
      url: "/about"
      weight: 50
